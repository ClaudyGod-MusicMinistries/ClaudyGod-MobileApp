{
  "name": "claudy-admin-workspace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup:web": "npm --prefix ./web install",
    "setup:api": "npm --prefix ../services/api install",
    "setup": "npm run setup:web && npm run setup:api",
    "dev:web": "npm --prefix ./web run dev",
    "dev:api": "npm --prefix ../services/api run dev",
    "build:web": "npm --prefix ./web run build",
    "preview:web": "npm --prefix ./web run preview"
  }
}
