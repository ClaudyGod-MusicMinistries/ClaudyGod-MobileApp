no_tty: true

pre-commit:
  interactive: false
  jobs:
    - name: repo staged checks
      run: yarn precommit:check

pre-push:
  interactive: false
  jobs:
    - name: full repo review (docker + backend + admin + mobile)
      run: yarn prepush:check
