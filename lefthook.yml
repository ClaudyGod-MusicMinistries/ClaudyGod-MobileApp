no_tty: true

pre-commit:
  interactive: false
  jobs:
    - name: repo staged checks
      run: bash ./scripts/precommit-all-staged.sh

pre-push:
  interactive: false
  jobs:
    - name: full repo review (docker + backend + admin + mobile)
      run: bash ./scripts/prepush-all-checks.sh
