no_tty: true

pre-commit:
  interactive: false
  jobs:
    - name: mobile lint
      run: yarn precommit:check

pre-push:
  interactive: false
  jobs:
    - name: mobile typecheck + lint
      run: yarn prepush:check
